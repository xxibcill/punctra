# Punctra 0.13.0-alpha.1 Repository Verification Record

Status: **Complete and repository-verified for the bounded persistent-terrain
slice; field activation, production-scale accuracy, true out-of-core adoption,
independent adoption, partner validation, and support qualification
outstanding**

Date: 2026-08-19

The final clean local qualification completed against the exact implementation
commit below. This record qualifies one bounded, explicit-AOI persistent
Surface slice around the existing full-AOI in-memory triangulator. It is not a
field, production-scale, true out-of-core, adoption, partner, or support claim.

The package version remains `0.13.0-alpha.1` under the
[Changelog](../../CHANGELOG.md)'s Unreleased heading. This record does not claim
a Git tag, a GitHub release, crates.io publication, or independent package
execution.

## Provenance

- Implementation commit: `bb0f097e6829b68cd2b0796f114908ed2e5f877a`
- v0.13 branch: `codex/persistent-production-scale-terrain-v0.13`
- Merged repository base: `50b0096d39cc5b43e06888d9e21d35dad72a01d9`
- Accepted design commit: `9dbb2c12e3a1c345a1f05a49a068a65985c4f2fe`
- Accepted design: [Persistent Bounded-AOI Terrain](../design/persistent-production-scale-terrain-v0.13.md)
- Package version: `0.13.0-alpha.1`

The implementation commit contains the code, tests, frozen fixtures, examples,
benchmark, and pre-attestation documentation that were qualified. This final
documentation-only attestation updates this record and the design's completion
pointer to pin that predecessor SHA rather than attempting to record its own
self-referential SHA.

## Verified environment

| Fact | Recorded value |
|---|---|
| Hardware model | `Mac17,9` |
| CPU | Apple M5 Pro |
| GPU | Apple M5 Pro, 16 cores, Metal 4; wgpu Metal backend |
| Memory | 24 GiB (`25,769,803,776` bytes) |
| Architecture | `arm64` |
| Operating system | macOS 26.5.2, build `25F84`; Darwin 25.5.0 |
| Filesystem | APFS |
| Rust | `rustc 1.90.0 (1159e78c4 2025-09-14)`, host `aarch64-apple-darwin` |
| Cargo | `cargo 1.90.0 (840b83a10 2025-07-30)` |

Only this exact local macOS/APFS/Apple-M5-Pro tier is repository-verified.
Linux, Windows, network filesystems, other GPUs, and other workstation tiers
remain unverified. The GPU commands below required an adapter and exercised
the Apple M5 Pro through wgpu's Metal backend.

## Qualified bounded surface

The qualified public seam prepares one immutable rebuildable Surface Artifact
for an explicit finite inclusive AOI. Its exact Snapshot, Workspace, Source,
Revision, Recipe/AOI, transform, structured metre/metre spatial reference,
terrain algorithm, counts, bounds, and canonical hashes are bound into
checksummed disk-v1 bytes. A file-backed handle exposes separately bounded
canonical vertex and face streams without retaining the whole Surface.

The four successful attempt dispositions are cold `Built`, verified-input
`ResumedInput`, verified-stage `ResumedPublication`, and compatible-target
`Opened`. Publication is descriptor-bound, no-replace, file-synced,
parent-synced, and revalidated. The verified stage and any work pathname remain
after publication because no portable unlink can be conditioned on the open
owned inode. A complete target takes precedence on warm open, and an arbitrary
work sibling is not trusted without verification.

The topology algorithm is still the canonical single-worker full-AOI
triangulator. Complete AOI sorting and topology remain memory-resident under
caller-declared hard limits. Persistence, bounded streams, a large containing
Source, and generated benchmark modes do not make this a true out-of-core
triangulator or establish a production AOI envelope.

## Frozen disk-v1 fixtures

The [checked-in manifest](../../crates/point-terrain/tests/fixtures/v1/manifest.json)
identifies each compatibility payload by relative path, exact byte length, and
BLAKE3 hash:

| Role | Path | Bytes | BLAKE3 |
|---|---|---:|---|
| Complete rebuildable Surface | `bounded-six-point.pterr` | 936 | `39d8f1decc8999f590d6dbefb3ea96748550a55dceea0a8ffdcf75a55f6c9b6c` |
| Resumable Ground-Input work | `bounded-six-point.pterr.surface-work-v1` | 608 | `49b24812750cac6cf3f54f074ad8f9eea877a9f61fe6fd3c98871e66a9d5d2ef` |
| Snapshot-binding Workspace manifest | `workspace/manifest.pwm` | 1,252 | `b116198600aa5f5f9403925c26c576c24b2e634cb88cf65db767ec3d1fa5c161` |

The six-Point Surface freezes six vertices, six faces, four hull vertices,
terrain algorithm 1, Surface disk 1, and these semantic hashes:

| Semantic fact | Frozen value |
|---|---|
| Input | `2e87d01015b87e5d93a8d9a0a409f36e44caa9f63ff614550163d80b4505c238` |
| Recipe | `8306b53b31c0f1bccd1ddb17d45be09ab652e1a6bd1e19ec25cbc5363bbe2b7d` |
| Geometry | `7f5f08f59b22d96b4b47477f9d44ada99a321f937fac0e8a0865b5d98872bc3a` |
| Topology | `fb8c17a2984dbb0cf0d87444c9f9b66e28c2ffbbfa4e3aca2bef9f362e33d63e` |
| Artifact | `5680cdd1765d9f950cd7d2c0429a0e668cbe9a701e0a459fc8986e30e92ca1f0` |

The frozen work fixture resumes with zero Source Point reads and reconstructs
the complete fixture exactly. These files are rebuildable Snapshot-derived
compatibility evidence, not Workspace authority or a production dataset.

## Local verification

No hosted CI was used. Every required command in the Local verification block
of [CONTRIBUTING.md](../../CONTRIBUTING.md) ran locally against the
implementation commit. Every required command passed.

| Command | Result |
|---|---|
| `cargo fmt --all --check` | PASS (exit 0) |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASS (exit 0) |
| `cargo test --workspace --all-features` | PASS (exit 0) |
| `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps` | PASS (exit 0) |
| `scripts/verify-packages.rb` | PASS (exit 0) |
| `cargo fmt --manifest-path fuzz/Cargo.toml --all --check` | PASS (exit 0) |
| `cargo clippy --manifest-path fuzz/Cargo.toml --all-targets -- -D warnings` | PASS (exit 0) |
| `cargo check --manifest-path fuzz/Cargo.toml --bin index_persistence` | PASS (exit 0) |
| `cargo check --manifest-path fuzz/Cargo.toml --bin terrain_persistence` | PASS (exit 0) |
| `cargo test --manifest-path fuzz/Cargo.toml --lib` | PASS (exit 0) |
| `cargo bench -p point-view --bench planner` | PASS (exit 0) |
| `cargo bench -p source-memory --bench read` | PASS (exit 0) |
| `cargo bench -p source-las --bench read` | PASS (exit 0) |
| `cargo bench -p point-index --bench index` | PASS (exit 0) |
| `cargo bench -p point-workspace --bench document` | PASS (exit 0) |
| `cargo bench -p point-review --bench review` | PASS (exit 0) |
| `cargo bench -p point-terrain --bench terrain` | PASS (exit 0) |
| `cargo bench -p terrain-demo --bench journal -- --save-baseline "qualification-pr14-final-bb0f097"` | PASS (exit 0) |
| `cargo bench -p renderer-demo --bench viewing` | PASS (exit 0) |
| `cargo run -p source-memory --example memory_source` | PASS (exit 0) |
| `cargo run -p point-index --example direct_use` | PASS (exit 0) |
| `cargo test -p point-workspace --all-features` | PASS (exit 0) |
| `cargo test -p point-review --test interface` | PASS (exit 0) |
| `cargo test -p render-protocol --test state_model` | PASS (exit 0) |
| `cargo run -p point-terrain --example derive` | PASS (exit 0) |
| `cargo run -p point-terrain --example persistent_surface` | PASS (exit 0) |
| `cargo test -p point-terrain --all-features` | PASS (exit 0) |
| `cargo test -p point-terrain --test persistence` | PASS (11 tests; exit 0) |
| `cargo test -p terrain-demo --lib --all-features` | PASS (105 tests; exit 0) |
| `cargo test -p terrain-demo --test workflow` | PASS (17 tests; exit 0) |
| `cargo test -p terrain-demo --test process` | PASS (10 tests; exit 0) |
| `cargo test -p renderer-demo --test headless_smoke` | PASS (13 tests; exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p renderer-demo --test headless_smoke corpus_success_binds_trace_inputs_and_separate_resource_measurements -- --exact` | PASS on Metal (exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p renderer-demo --bin renderer-demo appearance::gpu_tests` | PASS on Metal (exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p renderer-demo --test headless_smoke corpus_pre_v0_13_repository_lane_records_settlement_and_the_declared_matrix -- --exact` | PASS on Metal (exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p render-wgpu --test offscreen` | PASS on Metal (exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p renderer-demo --test planner` | PASS on Metal (exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p renderer-demo --test display_gpu` | PASS on Metal (exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo run -p render-wgpu --example third_party_host` | PASS on Metal (exit 0) |
| `test -f docs/guides/first-las-laz.md` | PASS (exit 0) |
| `test -f docs/guides/library-packaging.md` | PASS (exit 0) |
| `test -f docs/guides/persistent-terrain.md` | PASS (exit 0) |
| `ruby -rjson -e 'JSON.parse(File.read(ARGV.fetch(0)))' docs/guides/field-corpus.example.json` | PASS (exit 0) |
| `git diff --check` | PASS at the clean implementation commit (exit 0) |

### PR #14 remediation qualification

The exact implementation commit includes one focused commit for each reviewed
behavioral finding: canonical input-hash verification, compatible publication
race disposition, an independent canonical-vertex ceiling, and complete
nested Point-row limit reporting. Two later focused documentation commits make
the `Opened` race meaning and independent input/vertex ceilings explicit.

The clean full matrix above ran only after those commits were complete. The
final attestation changes only this verification record and the design's
completion pointer; it does not change the qualified implementation or package
payload.

### Focused acceptance counts

The point-terrain package run reported 51 core tests, 11 persistence integration
tests, five public persistence-acceptance tests, and three passing frozen-v1
fixture tests plus one explicitly gated ignored fixture-regeneration test. The
terrain-demo run reported 105 library tests, 17 workflow tests, and 10 process
tests. The renderer-demo headless suite reported 13 tests.

The persistence suites cover deterministic bytes across Source row batches;
legacy/persistent topology equality; all four dispositions; zero-row warm open;
Snapshot, Recipe/AOI, algorithm, transform, and spatial-reference staleness;
complete/work truncation and corruption; invalid offsets, order, winding,
references, counts, hashes, and checksums; cancellation and I/O faults at
durable boundaries; publication certainty and races; independent inclusive and
one-under resource limits; and bounded multi-block vertex/face streams with
post-yield mutation detection.

## Generated example observations

The default `persistent_surface` example generated 10,000 Ground Input Points,
10,000 vertices, 19,602 faces, and 396 hull vertices. It reported 521,494
topology steps, 135,790,592 algorithm-accounted peak topology working bytes, a
320,480-byte verified input checkpoint, a 556,088-byte complete Surface, an
876,568-byte peak logical work-plus-stage total, and 957 bytes of retained
handle metadata.

A forced one-byte Artifact ceiling retained the verified checkpoint without
publishing a Surface. `ResumedInput` then reused all 10,000 Ground Input Points
with zero Snapshot Point reads and reproduced the uninterrupted Artifact bytes
exactly. Bounded streams consumed all vertices in three batches and all faces
in five batches. The final compatible `Opened` attempt consumed zero Snapshot
Point rows and reported no topology observations.

The example reported independent 10,000,000-Point Ground Input and
10,000,000-vertex ceilings. Its nested Point-row report named candidate limits
of 4,194,303 visited nodes, 65,536 output spans, 18,446,744,073,709,551,615
candidate Points, and 16,777,216 working bytes; Source-read limits of 65,536
spans, 50,000,000
Points, 65,536 batch Points, 8,388,608 batch payload bytes, and 16,777,216
adapter working bytes; and row-stream limits of 100,000 overlay segments,
4,294,967,296 overlay bytes, 10,000,000 output Points, 65,536 batch Points,
4,194,304 batch payload bytes, and 134,217,728 working bytes.

Per-invocation debug example elapsed values are intentionally not copied. Direct
stage bytes, worker heap, process peak resident memory, allocated filesystem
blocks, QA, LandXML, View, and field accuracy remained explicit JSON `null`
observations. The example does not infer them from adjacent counters.

## Generated benchmark observations

All values below use the documented default generated scales. The estimate is
Criterion's slope estimate when available and its mean estimate otherwise;
parentheses contain the 95% confidence interval. Criterion comparison labels
are informational and were not qualification gates.

| Package / case | Generated scale | Estimate (95% CI) |
|---|---:|---:|
| `point-view` / perspective plan | 5,461 nodes / 16,777,216 logical Points | 850.53 us (843.77–858.69 us) |
| `point-view` / orthographic plan | 5,461 nodes / 16,777,216 logical Points | 795.74 us (794.32–797.39 us) |
| `source-memory` / bounded pre-opened read | 262,144 Points | 442.38 us (441.71–443.15 us) |
| `source-las` / LAS sequential point rate | 1,000,000 Points | 20.194 ms (20.179–20.213 ms) |
| `source-las` / LAS sequential source-byte rate | 1,000,000 Points | 20.431 ms (20.405–20.476 ms) |
| `source-las` / LAZ sequential point rate | 1,000,000 Points | 143.22 ms (143.03–143.43 ms) |
| `source-las` / LAZ sequential source-byte rate | 1,000,000 Points | 145.65 ms (145.46–145.86 ms) |
| `source-las` / LAS full open | 1,000,000 Points | 39.022 ms (38.893–39.140 ms) |
| `source-las` / LAZ full open | 1,000,000 Points | 146.89 ms (146.67–147.11 ms) |
| `point-index` / cold build | 1,000,000 Points | 335.97 ms (332.48–338.77 ms) |
| `point-index` / warm open | 1,000,000 Points | 24.084 ms (24.064–24.107 ms) |
| `point-index` / whole-bounds candidates | 1,000,000 Points | 615.28 ns (613.96–616.64 ns) |
| `point-index` / root display read | 1,000,000 Points | 1.2874 ms (1.2838–1.2913 ms) |
| `point-index` / complete leaf read | 1,000,000 Points | 138.06 us (137.32–138.87 us) |
| `point-workspace` / 0% resident selection | 1,000,000 Points | 15.506 us (15.405–15.688 us) |
| `point-workspace` / 1% resident selection | 1,000,000 Points | 3.4044 ms (3.3691–3.4545 ms) |
| `point-workspace` / 50% resident selection | 1,000,000 Points | 16.606 ms (16.578–16.632 ms) |
| `point-workspace` / 100% resident selection | 1,000,000 Points | 32.501 ms (32.366–32.631 ms) |
| `point-workspace` / 100% forced-spill selection | 1,000,000 Points | 1.0675 s (1.0549–1.0817 s) |
| `point-review` / resident screen-through | 20,000 Points | 1.6788 ms (1.6725–1.6840 ms) |
| `point-review` / forced-spill screen-through | 20,000 Points | 7.2494 ms (7.1957–7.3006 ms) |
| `point-terrain` / persistent cold prepare | 10,000 vertices / 19,602 faces | 160.97 ms (159.15–163.12 ms) |
| `point-terrain` / persistent warm open | 10,000 vertices / 19,602 faces | 44.823 ms (44.632–44.962 ms) |
| `point-terrain` / legacy derive | 10,000 vertices / 19,602 faces | 12.923 ms (12.843–12.997 ms) |
| `point-terrain` / detached QA | 3 Check Points / 19,604 face tests | 109.15 us (108.71–109.93 us) |
| `point-terrain` / LandXML export | 10,000 vertices / 19,602 faces | 12.620 ms (12.417–12.816 ms) |
| `terrain-demo` / cold start | 10,000 Points / 8 frames | 139.56 ms (138.47–140.51 ms) |
| `terrain-demo` / committed-Edit resume | 10,000 Points / 8 frames | 108.68 ms (107.67–110.13 ms) |
| `terrain-demo` / Retryable-intent resume | 10,000 Points / 8 frames | 127.66 ms (126.65–128.68 ms) |
| `terrain-demo` / LandXML and report reconciliation | 10,000 Points / 8 frames | 92.349 ms (91.563–93.570 ms) |
| `terrain-demo` / complete revalidation | 10,000 Points / 8 frames | 78.753 ms (77.907–79.702 ms) |
| `renderer-demo` / warm index open | 100,000 Points | 1.7720 ms (1.7606–1.7853 ms) |
| `renderer-demo` / first indexed display batch | 100,000 Points | 1.3231 ms (1.3168–1.3322 ms) |

The source-memory run's interval corresponds to 591.55–593.47 million
elements per second. LAS and LAZ measured 372,406 and 2,588,206 peak heap bytes
against a 33,554,432-byte ceiling. The index run produced a 1,971,528-byte
Artifact and measured 262,616 peak heap bytes. Workspace Point-ID iteration
measured 2,621,440 peak bytes; the forced-spill run measured a 114,688-byte RSS
delta and 9,009,182 temporary bytes.

The persistent Terrain resource report separately observed 159,687 us for its
cold attempt, 43,738 us for warm open, and 813 us for complete bounded stream
consumption. It reported the same 10,000 vertices, 19,602 faces, 396 hull
vertices, 556,088 Artifact bytes, 876,568 peak temporary bytes, 135,790,592
accounted cold working bytes, and 521,494 topology steps while also asserting
equality with the legacy topology. Its work-checkpoint and direct-stage byte
observations remained explicit JSON `null`. Those one-run resource observations
are not substituted for the Criterion intervals.

The durable terrain-demo journal was 2,804 bytes across eight frames, and its
canonical report was 11,539 bytes and contained 116 semantic limit facts. The
renderer viewing fixture produced a
131,816-byte index Artifact and observed 525,456 temporary index bytes. These
are generated local observations, not service-level objectives or field
measurements.

## Nonclaims and remaining exits

This repository record does not establish a field-measured AOI, Ground-Point
count, latency, memory, temporary-storage, accuracy, or workstation envelope.
Neither required above-500-million-Point Source project from an unrelated firm
has completed its declared AOI and accepted accuracy baseline. No licensed or
sanitized production corpus was used to qualify Terrain scale.

The release does not add external-memory, tiled, distributed, parallel, GPU,
or approximate triangulation. It does not establish independent example use,
crates.io publication, Autodesk Civil 3D or Bentley execution, vendor
certification, partner acceptance, paid use, conversion, time savings, reduced
rework, supported installers, operational support, or an SLA.

Complete and repository-verified for the bounded persistent-terrain slice;
field activation, production-scale accuracy, true out-of-core adoption,
independent adoption, partner validation, and support qualification
outstanding.
