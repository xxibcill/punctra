# Punctra 0.21.0-alpha.1 Repository Verification Record

Status: **Complete and repository-verified for the bounded visual-quality
baseline, regression corpus, packed browser continuation, and one exact local
Chromium/macOS/Apple-GPU lane; broader browser/device support, physical-display
presentation, independent human interpretation, independent adoption,
improved or final visual quality, API stability, support qualification, beta,
v1, and release-candidate status remain outstanding**

This record closes only the repository implementation accepted by the
[v0.21 Visual-Quality Baseline and Regression Corpus
design](../design/visual-quality-baseline-v0.21.md). The attended maintainer
lane and its non-gating rubric are repository evidence, not independent-human,
professional-usability, or adopter evidence.

## Provenance

- Branch: `codex/v0.21-visual-quality-baseline`
- Implementation commit: `124fab07ab993059ef5df179cb227f7b0ba9b19e`
- Qualification verifier SHA-256: `3bc9ef7ad67c54903c16090ddd0ae032228ba02ba9a9f888228e65ebab32b336`
- Visual verifier: 153,403 bytes, SHA-256 `5c0fdbe41917cc40adf9d22f0af4d76a220a23c1b1e7875c53efb300bd33c837`
- Package version: `0.21.0-alpha.1`
- Observed on: 2026-08-28
- Integration baseline: [v0.21 browser baseline](v0.21-browser-baseline.json)
- Quickstart evidence: [v0.21 packed browser quickstart](v0.21-browser-quickstart.json)
- Browser evidence: [v0.21 browser matrix](v0.21-browser-matrix.json)
- Visual policy: [v0.21 visual baseline](v0.21-browser-visual-baseline.json),
  31,851 bytes, SHA-256
  `439a83ce9abcdbbcbb84b9a65a7553c00c8fea0a0ec9bc01275dc43383cb1d3b`
- Visual evidence: [v0.21 attended visual evidence](v0.21-browser-visual-evidence.json),
  22,731,985 bytes, SHA-256
  `b703aa03524725f03ad2130d68ad6f9e663805e73d56d4cbf77d0c57a4b241ec`
- Visual PNG artifacts: [v0.21 visual artifact directory](v0.21-browser-visual-artifacts/)
- Generated declarations: [browser SDK API reference](../api/browser-sdk.md)

The implementation pin is the immutable target for the package, quickstart,
browser qualification, baseline-input, capture, comparison, visual evidence,
and verifier facts below. The documentation-only release record follows that
pin; it does not alter any qualified implementation path or observed artifact.

## Local environment

| Fact | Observation |
|---|---|
| Operating system | macOS 26.6.2 build 25G83, arm64 |
| Device | Apple silicon laptop, Apple M5 Pro, 16-core integrated GPU, Metal 4 |
| Browser | Codex in-app browser, Chromium user-agent 151.0.0.0 |
| Browser screen | 1,512 × 982 CSS pixels, 30-bit color/pixel depth |
| Functional canvas | 874.28125 × 546.421875 CSS pixels, DPR 2; 1,749 × 1,093 physical pixels |
| Canonical visual canvas | 320 × 240 CSS pixels, requested and observed DPR 2; 640 × 480 physical pixels |
| WebGPU | BrowserWebGpu, Bgra8Unorm, sRGB, Opaque, fifo; no fallback |
| Rust / Cargo | rustc 1.90.0 (1159e78c4 2025-09-14) / cargo 1.90.0 (840b83a10 2025-07-30) |
| Node / npm | Node.js 24.16.0 / npm 10.9.8 |

The browser-reported CSS screen size and bit depth are exact session facts; the
host independently reported the built-in Retina display path. The browser
exposed a generic WebGPU adapter name, so the Apple M5 Pro mapping is a local-
system inference from the sole installed GPU. Neither fact establishes a
general browser, adapter, compositor, or physical-display class.

## Packed consumer evidence

`scripts/build-browser-sdk.sh` produced the local `@punctra/viewer` and
`@punctra/react` tarballs. `scripts/verify-browser-sdk.mjs` installed only those
artifacts into fresh TypeScript and React consumers, type-checked them, built
development and production graphs, verified the emitted Wasm and module Worker,
and published the packed quickstart under `target/browser-quickstart`.

The attended quickstart published:

| Fact | Observation |
|---|---|
| Schema | `punctra-browser-quickstart-acceptance-v1` |
| Package | `0.21.0-alpha.1` |
| Viewer artifact SHA-256 | `a30022f2391bf5112bc3f5aa146da35b964c359dfc9a702ecbdc748dca4cd814` |
| Source identity | `c459ff39717b7d6994aaebf344641f5a3add7faf65e249b85933ebd066d1c26e` |
| Coverage | sampled, 4,096 displayed Points |
| Display modes | neutral, elevation, RGB, intensity, classification |
| Projections | orthographic, perspective |
| Cancellation | viewer and last frame retained after delayed-load cancellation |
| Recoverable failure | disconnected manifest retained the viewer; corrected retry succeeded |
| Recreation-required failure | post-publication cancellation fused the viewer; recreation and reload succeeded |
| Pick / exact authority | `provisional_gpu_hint` / `exact_source_record` |
| Lifecycle | pause/resume passed; viewer disposed |

## Exact browser qualification

The packed browser harness passed the one checked-in v0.21 runtime lane. It
loaded the 70,000-Point immutable Source, displayed its 4,096-Point sampled
root in four batches, exercised every accepted lifecycle, input, presentation,
exactness, recovery, latency, and independent resource gate, and published
`punctra-browser-qualification-v1`.

| Measurement | Observed | Ceiling |
|---|---:|---:|
| Cold first sampled Coverage | 16.300 ms | 10,000 ms |
| Cold settled View | 18.100 ms | 15,000 ms |
| Warm first sampled Coverage | 12.600 ms | 10,000 ms |
| Warm settled View | 14.800 ms | 15,000 ms |
| Foreground callback interval p95 | 17.200 ms | 50 ms |
| Main-thread frame submission p95 | 0.600 ms | 16.7 ms |
| Cancellation acknowledgement | 7.600 ms | 1,000 ms |
| Renderer resident bytes | 98,304 bytes | 196,608 bytes |
| Retained decoded records | 131,072 bytes | 262,144 bytes |
| Worker staging high-water | 204,800 bytes | 327,680 bytes |
| Concurrent response high-water | 172,032 bytes | 262,144 bytes |
| Verified persistent cache | 172,696 bytes | 4,194,304 bytes |
| Functional canvas | 4,874,832 bytes | 33,554,432 bytes |
| Transient textures | 9,749,664 bytes | 67,108,864 bytes |

The non-standard `performance.memory.usedJSHeapSize` observation had a
30,471,302-byte phase high-water. Process RSS, physical cache allocation, and
physical GPU/driver allocation were unavailable and remain explicit nulls.
Callback intervals include browser/callback scheduling delay, and main-thread
submission intervals are not physical GPU-completion or display-panel timing.

Pre-publication Worker and network failures retained the viewer and generation;
retry succeeded after correction. Cancellation retained the last viewer/frame.
Over-limit resize retained the prior viewport. Partial publication and device
loss remain recreation-required and are covered deterministically rather than
forced destructively on the physical adapter. Stale generations were rejected.

## Visual baseline and regression evidence

The attended workflow ran sequentially. Record mode created the nine canonical
baseline PNGs and commit-free `baseline-inputs.json`; those inputs crossed the
implementation pin. The pinned build then repeated the packed quickstart and
browser qualification before verify mode compared fresh captures against the
checked-in baselines. Only the verify-mode record below is final evidence.

| Fact | Observation |
|---|---|
| Evidence schema / mode | `punctra-browser-visual-evidence-v1` / `verify` |
| Attended lane | `codex-iab-chromium-151-macos-26-apple-m5-pro` |
| Execution / qualification | `visible_user_gesture` / `exact_observed_lane_only` |
| Trial result | 9 completed, 9 passed, 0 failed |
| Recreations | 3 complete viewer/harness recreations per trial |
| Quiet / transition windows | 30 quiet foreground frames; fixed nine-step mixed-LOD trace |
| Canonical capture | top-left linear RGBA8, lossless PNG, 640 × 480 physical pixels |
| Artifact result | 873 PNGs, 12,203,302 encoded bytes; aggregate gate passed |
| Baseline inputs | 5,100 bytes, SHA-256 `ec45107078bb90c58964f85e13316953bdaf3db1576b7fdfb6f9e38304c3c49a` |
| Pixel tolerance caps | channel threshold 2; unstable fraction 0.001; maximum delta 4 |
| Feature displacement cap | 1 physical pixel |
| Final summary | image, temporal, Coverage, feature, authority, resource, cleanup, provenance, and rubric gates passed |

Every fixed trial passed:

| Trial | Input / display purpose | Result |
|---|---|---|
| `generated-neutral-mixed-lod-perspective` | generated neutral, large-world, mixed-LOD trace | PASS |
| `generated-elevation-layered-orthographic` | generated elevation and depth layers | PASS |
| `generated-rgb-hdr-perspective` | generated high-dynamic-range RGB | PASS |
| `generated-intensity-sparse-orthographic` | generated sparse intensity structure | PASS |
| `generated-classification-selection-perspective` | generated classification and presentation highlight | PASS |
| `autzen-rgb-perspective` | permission-bound Autzen derivative, RGB | PASS |
| `autzen-classification-perspective` | permission-bound Autzen derivative, classification | PASS |
| `autzen-intensity-perspective` | permission-bound Autzen derivative, intensity | PASS |
| `autzen-elevation-perspective` | permission-bound Autzen derivative, elevation | PASS |

The eleven exact post-capture rubric image presentations (six unique PNGs
across six rubric prompts) loaded visibly before submission. The anonymous
session label was
`codex-local-maintainer-not-human`. Every non-gating outcome was explicitly
`not_observed`: depth, shape, density transition, color meaning, selection, and
false feature. This is a complete truthful rubric record, not a favorable
visual judgment, independent-human observation, professional-usability result,
or claim that visual quality improved.

The canonical images are presentation-only renderer evidence from private
offscreen GPU readback. They do not observe operating-system composition, color
management after the capture target, or a physical display panel, and they
cannot change Point Identity, Source geometry, Coverage, selection, or exact
Query authority. The one-TAR Blob/local-server mechanisms and any export
receipt are private transport only and are not evidence.

## Local verification matrix

The final pinned checkout passed every relevant command in `CONTRIBUTING.md`:

| Lane | Result |
|---|---|
| Formatting and Clippy with warnings denied | PASS |
| All-feature workspace tests and rustdoc | PASS |
| JavaScript, package, quickstart, qualification, archive, provenance, capture, comparison, PNG, rubric, and verifier tests | PASS |
| Wasm browser build plus generated SDK reference check | PASS |
| Packed TypeScript and React clean-consumer builds | PASS |
| Static integration-baseline, qualification-matrix, and visual-evidence verification | PASS |
| Package archive verification for all public Rust libraries | PASS |
| Fuzz formatting, check, and stable harness tests | PASS |
| Documented examples and focused process/fixture regressions | PASS |
| All documented benchmark commands and fixed resource gates | PASS |
| `PUNCTRA_REQUIRE_GPU=1` native GPU acceptance and renderer example | PASS |
| Documentation/file/JSON/link checks and `git diff --check` | PASS |
| Attended record, packed quickstart, exact browser qualification, and verify workflow | PASS |

No hosted CI was added or used.

## Frozen visual boundary

The `punctra-browser-visual-baseline-v1` verifier derives and checks:

- exact generated and permission-bound derivative inputs, attribution, and
  deterministic trial identities;
- the checked-in baseline-input manifest and nine canonical baseline PNGs;
- the implementation commit, qualified paths, package/runtime bytes, verifier
  byte length, and verifier SHA-256;
- cameras, projections, display modes, highlight state, settlement, canonical
  capture layout, and unchanged point-appearance policy;
- decoded-pixel, exact generated temporal, Coverage, feature-location,
  authority, resource, timing, cleanup, and artifact identities;
- three complete recreations for all nine trials and all 873 PNG artifacts;
- post-capture rubric presentation, selection order, session label, and exact
  preserved outcomes; and
- all explicit unavailable measurements and external-evidence nonclaims.

The verifier derives pass/fail from the evidence and checked-in artifacts; a
recorded `passed` flag cannot override a failed gate. The accepted public SDK
surface is unchanged: v0.21 adds no screenshot, arbitrary-scene, capture,
comparison, or renderer-target package export.

## Outstanding external and later evidence

The following remain false or unobserved:

- independent-human interpretation, professional usability, or an independent
  adopter completing the embedding and visual workflow;
- registry/CDN publication, production hosting/authentication/CSP, or support
  operations;
- browser, operating-system, adapter, device, mobile, compositor, or physical-
  display qualification outside the exact one-entry matrix;
- arbitrary Sources, cross-browser visual equivalence, physical GPU-completion
  timing, process RSS, driver/GPU allocation, energy, or thermal evidence;
- improved or final visual quality, intentional point-appearance changes, or a
  favorable interpretation result; and
- API stability, a compatibility promise, support qualification, beta, release
  candidate, or v1.

No known release-blocking correctness, security, data-mixing, lifecycle,
recovery, packaging, visual-baseline, or documentation defect remains inside
the declared bounded repository slice. That statement does not promote any
outstanding external evidence or broaden the exact attended lane.
