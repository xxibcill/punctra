# Punctra 0.9.0 Repository Verification Record

Status: **Complete — repository-verified version-1 compatibility candidate**

Date: 2026-08-13

Every command below exited successfully at the named verification commit.
Punctra 0.9.0 qualifies the repository's narrow version-1 compatibility
surface. It is not a product-readiness, partner-acceptance, or production-
support claim.

## Provenance

- Verification commit: `cbcb69ed09cd0417e137a65eba8bd8c6ee186c1f`
- v0.9 branch: `codex/trust-v1-candidate-v0.9`
- Inherited v0.8 repository base: `9a8363a0d807990209f8252d93229c7f9464c923`
- Package version: `0.9.0`

The v0.8 repository interoperability-qualification slice is Complete. Its
external design-partner product-MVP gates remain outstanding. v0.9 hardens and
freezes that repository surface; it does not convert repository evidence into
external product evidence.

## Verified environment

| Fact | Recorded value |
|---|---|
| Hardware model | `Mac17,9` |
| CPU | Apple M5 Pro, 15 physical cores |
| GPU | Apple M5 Pro, 16 cores, Metal 4; wgpu Metal backend |
| Memory | 25,769,803,776 bytes |
| Architecture | `arm64` |
| Operating system | macOS 26.5.2, build `25F84`; Darwin 25.5.0 |
| Filesystem | APFS Data volume `/dev/disk3s5`, internal Apple Fabric SSD, 4,096-byte allocation and device blocks |
| Rust | `rustc 1.90.0 (1159e78c4 2025-09-14)`, host `aarch64-apple-darwin` |
| Cargo | `cargo 1.90.0 (840b83a10 2025-07-30)` |

Only this exact local macOS/APFS/Apple-M5-Pro tier is verified. Linux,
Windows, network filesystems, other GPUs, and other platform/filesystem tiers
remain unverified. Supplementary compile or container checks do not promote a
support tier.

## Frozen compatibility surface

| Owner | Frozen version or schema |
|---|---|
| `point-source` | SourceRecord schema 1 and Source contract 1 |
| `point-index` | disk 1 and recipe 1 |
| `point-workspace` | disk 1 and semantic 1 |
| `point-terrain` | Terrain algorithm 1; metric-metre LandXML 1.2 single-TIN subset |
| `terrain-demo` | Run disk 1, semantic 1, and frame 1 |
| `terrain-demo` report | `punctra.terrain-workflow.audit.v1` |
| `terrain-demo` evidence | `punctra.terrain-demo.landxml-round-trip-evidence.v1` |

There is no version-2 persisted format and no migration framework in v0.9.
Authoritative version-1 bytes remain readable and fail closed on unknown
versions. The version-1 index is rebuildable under the explicit policy in the
[support matrix](../architecture/v0.9-support-matrix.md). Temporary files have
no cross-release readability promise.

The checked-in manifests are owned locally by `point-source`, `source-memory`,
`source-las`, `point-index`, `point-workspace`, and
`terrain-demo/tests/fixtures/qualification-v1`. They pin relative paths,
support classes, lengths, BLAKE3 hashes, versions, identities, and semantic
facts. Authoritative fixtures reopen without changing checked-in bytes; index
fixtures exercise open/resume/rebuild policy; Run prefixes, report, LandXML,
and canonical pass/fail evidence match their manifest.

## Local verification

No hosted CI was used. Every command ran locally at the verification commit.

| Command | Result |
|---|---|
| `cargo fmt --all --check` | PASS (exit 0) |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | PASS (exit 0) |
| `cargo test --workspace --all-features` | PASS (exit 0) |
| `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps` | PASS (exit 0) |
| `cargo fmt --manifest-path fuzz/Cargo.toml --all --check` | PASS (exit 0) |
| `cargo check --manifest-path fuzz/Cargo.toml --bin index_persistence` | PASS (exit 0) |
| `cargo test --manifest-path fuzz/Cargo.toml --lib` | PASS (2 tests; exit 0) |
| `cargo bench -p point-view --bench planner` | PASS (exit 0) |
| `cargo bench -p source-memory --bench read` | PASS (exit 0) |
| `cargo bench -p source-las --bench read` | PASS (exit 0) |
| `cargo bench -p point-index --bench index` | PASS (exit 0) |
| `cargo bench -p point-workspace --bench document` | PASS (exit 0) |
| `cargo bench -p point-terrain --bench terrain` | PASS (exit 0) |
| `cargo bench -p terrain-demo --bench journal` | PASS (exit 0) |
| `cargo run -p source-memory --example memory_source` | PASS (exit 0) |
| `cargo run -p point-index --example direct_use` | PASS (exit 0) |
| `cargo test -p point-workspace --all-features` | PASS (exit 0) |
| `cargo run -p point-terrain --example derive` | PASS (exit 0) |
| `cargo test -p point-terrain --all-features` | PASS (exit 0) |
| `cargo test -p terrain-demo --test workflow` | PASS (15 tests; exit 0) |
| `cargo test -p terrain-demo --test process` | PASS (9 tests; exit 0) |
| `cargo test -p renderer-demo --test headless_smoke` | PASS (3 tests; exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p render-wgpu --test offscreen` | PASS (8 tests on Metal; exit 0) |
| `PUNCTRA_REQUIRE_GPU=1 cargo test -p renderer-demo --test planner` | PASS (1 test on Metal; exit 0) |

## Generated benchmark observations

All estimates and 95% confidence intervals below use the documented default
generated scales. The value is Criterion's slope estimate when available and
its mean estimate otherwise. These are observations on the recorded machine,
not service-level objectives or production-data evidence. Criterion's saved-
baseline comparison labels are informational and were not release thresholds.

| Package / case | Scale | Estimate (95% CI) |
|---|---:|---:|
| `point-view` / quadtree plan | 5,461 nodes / 16,777,216 logical Points | 32.531 ms (32.133–32.988 ms) |
| `source-memory` / bounded pre-opened read | 262,144 Points | 565.631 µs (532.490–601.968 µs) |
| `source-las` / LAS sequential point rate | 1,000,000 Points | 21.889 ms (21.542–22.493 ms) |
| `source-las` / LAS sequential source-byte rate | 1,000,000 Points | 21.847 ms (21.646–22.295 ms) |
| `source-las` / LAZ sequential point rate | 1,000,000 Points | 143.787 ms (143.079–145.195 ms) |
| `source-las` / LAZ sequential source-byte rate | 1,000,000 Points | 150.738 ms (141.804–165.460 ms) |
| `source-las` / LAS full open | 1,000,000 Points | 41.805 ms (41.547–41.944 ms) |
| `source-las` / LAZ full open | 1,000,000 Points | 145.716 ms (144.722–146.994 ms) |
| `point-index` / cold build | 1,000,000 Points | 337.892 ms (336.738–339.381 ms) |
| `point-index` / warm open | 1,000,000 Points | 24.762 ms (24.655–24.936 ms) |
| `point-index` / whole-bounds candidates | 1,000,000 Points | 637.043 ns (634.120–640.438 ns) |
| `point-index` / root display read | 1,000,000 Points | 1.303 ms (1.300–1.305 ms) |
| `point-index` / complete leaf read | 1,000,000 Points | 134.993 µs (134.858–135.111 µs) |
| `point-workspace` / 0% resident selection | 1,000,000 Points | 15.626 µs (15.459–15.961 µs) |
| `point-workspace` / 1% resident selection | 1,000,000 Points | 3.353 ms (3.338–3.367 ms) |
| `point-workspace` / 50% resident selection | 1,000,000 Points | 16.621 ms (16.500–16.802 ms) |
| `point-workspace` / 100% resident selection | 1,000,000 Points | 32.257 ms (32.162–32.479 ms) |
| `point-workspace` / 100% forced-spill selection | 1,000,000 Points | 1.085 s (1.070–1.101 s) |
| `point-terrain` / derive | 10,000 vertices / 19,602 faces | 12.722 ms (12.688–12.761 ms) |
| `point-terrain` / detached QA | 3 check Points / 19,604 face tests | 109.165 µs (108.733–109.812 µs) |
| `point-terrain` / LandXML export | 10,000 vertices / 19,602 faces | 12.915 ms (12.508–13.486 ms) |
| `terrain-demo` / cold start | 10,000 Points / 8 frames | 141.609 ms (139.315–144.036 ms) |
| `terrain-demo` / committed-edit resume | 10,000 Points / 8 frames | 109.205 ms (106.860–111.416 ms) |
| `terrain-demo` / retryable-intent resume | 10,000 Points / 8 frames | 128.453 ms (124.385–131.975 ms) |
| `terrain-demo` / LandXML and report reconciliation | 10,000 Points / 8 frames | 92.776 ms (91.074–94.432 ms) |
| `terrain-demo` / complete revalidation | 10,000 Points / 8 frames | 79.788 ms (79.284–80.370 ms) |

The LAS and LAZ read paths measured peak heap at 372,435 and 2,588,235 bytes,
respectively, below their 33,554,432-byte ceiling. The index candidate/root/
leaf path measured 262,404 bytes against the same ceiling. Workspace Point-ID
iteration measured 2,621,440 peak bytes and zero retained bytes; its forced-
spill case measured an 81,920-byte peak RSS delta and 9,009,182 temporary
bytes. Terrain descriptor accounting reported 135,790,592 peak working bytes
and 1,034,176 retained surface bytes, below its 1-GiB and 2-GiB limits. These
accounted or process-level observations are not a claim about total worker-heap
usage where the benchmark explicitly leaves that measurement unclaimed.

## Nonclaims

This release does not establish licensed or sanitized production-data
evidence, a Terrain run above 500 million Points, observed Autodesk Civil 3D
or Bentley execution, vendor certification, firm acceptance, a paid pilot or
paid use, conversion, measured labor savings, product packaging, an installer
or updater, an operational support team, or an SLA. Caller-declared downstream
application settings are declarations, not proof that the application ran.
