import type { DisplayMode } from "@punctra/viewer";

export const QUICKSTART_DISPLAY_MODES: readonly DisplayMode[] = Object.freeze([
  "neutral",
  "elevation",
  "rgb",
  "intensity",
  "classification",
]);
